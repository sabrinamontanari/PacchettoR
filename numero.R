numero <- function(x) {                               #su R: positivo(numero incognito)
      if(x>0) {
      "Questo numero è positivo"
      }
      else if(x<0){
      "Questo numero è negativo"}
      else {                              #oppure else(x=0)
      "Zero non è positivo nè negativo"
      }
}
